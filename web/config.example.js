// Copy this file to config.js and fill in your Supabase project details.
// The anon key is safe to publish: it can only read what row-level
// security (see ../supabase/schema.sql) allows, which is the
// name-and-status-only `presence_board` view.
export const SUPABASE_URL = 'https://YOUR-PROJECT.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR-ANON-PUBLIC-KEY';
